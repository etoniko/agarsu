{
  "name": "agarsu-client",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Agar.su client — single bundle, self-hosted assets, deferred ads; Mail.ru skipped when authorized",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
