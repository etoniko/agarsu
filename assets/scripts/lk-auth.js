/* Moved into main.js — keep this stub so old cached HTML does not break. */
(function () {
  if (window.AgarLkAuth) return;
  console.warn("[agar.su] lk-auth.js is deprecated; auth lives in main.js");
})();
